apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations: {}
  labels:
    role: ingestion
  name: ingest-demo
  namespace: staging-environment
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ingest-demo
    spec:
      containers:
      - env:
        - name: INGEST_API
          value: http://api.ingest.staging.data.humancellatlas.org:80
        image: quay.io/humancellatlas/ingest-demo:v0.5.1.rc
        imagePullPolicy: Always
        name: ingest-demo
        ports:
        - containerPort: 5000
      dnsPolicy: Default
