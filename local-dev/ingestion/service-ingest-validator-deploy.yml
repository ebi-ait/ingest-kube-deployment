---
apiVersion: v1
kind: Service
metadata:
  labels:
    role: ingestion
  name: ingest-validator-service
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: ingest-validator
  sessionAffinity: None
  type: NodePort