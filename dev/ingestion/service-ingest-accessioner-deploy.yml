---
apiVersion: v1
kind: Service
metadata:
  labels:
    role: ingestion
  name: ingest-accessioner-service
  namespace: dev-environment
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: ingest-accessioner
  sessionAffinity: None
  type: LoadBalancer