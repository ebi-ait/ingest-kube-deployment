apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    role: ingestion
  name: redis-service
  namespace: dev-environment
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: mongo
  sessionAffinity: None
  type: ClusterIP
