apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations: {}
  labels:
    role: ingestion
  name: ingest-core
  namespace: {{ .Values.environment }}-environment
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ingest-core
    spec:
      containers:
      - env:
        - name: MONGO_URI
          value: mongodb://${MONGO_SERVICE_SERVICE_HOST}:${MONGO_SERVICE_SERVICE_PORT}/admin
        - name: RABBIT_HOST
          value: ${RABBIT_SERVICE_SERVICE_HOST}
        - name: RABBIT_PORT
          value: ${RABBIT_SERVICE_SERVICE_PORT_SERVICE}
        - name: SCHEMA_BASE_URI
          value: https://schema.humancellatlas.org/
        - name: STATE_TRACKER_HOST
          value: $(INGEST_STATE_TRACKING_SERVICE_SERVICE_HOST)
        - name: STATE_TRACKER_PORT
          value: $(INGEST_STATE_TRACKING_SERVICE_SERVICE_PORT)
        - name: GCP_PROJECT_WHITELIST
          value: "hca-dcp-pipelines-prod.iam.gserviceaccount.com,hca-dcp-production.iam.gserviceaccount.com,human-cell-atlas-travis-test.iam.gserviceaccount.com,broad-dsde-mint-dev.iam.gserviceaccount.com,broad-dsde-mint-test.iam.gserviceaccount.com,broad-dsde-mint-staging.iam.gserviceaccount.com" 
        - name: SVC_AUTH_AUDIENCE
          value: https://data.humancellatlas.org/
        - name: USR_AUTH_AUDIENCE
          value: https://dev.data.humancellatlas.org/
        - name: AUTH_ISSUER
          value: https://humancellatlas.auth0.com/
        image: quay.io/humancellatlas/ingest-core:{{ .Values.image }}
        imagePullPolicy: Always
        name: ingest-core
        ports:
        - containerPort: 8080
