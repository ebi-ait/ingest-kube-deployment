apiVersion: v1
kind: Service
metadata:
  labels:
    role: ingestion
  name: ingest-locusts-service
  namespace: {{ .Values.environment }}-environment
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8089
  selector:
    app: ingest-api-tests
  sessionAffinity: None
  type: NodePort
